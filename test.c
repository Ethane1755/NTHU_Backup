#include <stdio.h>

int main(void) {

    int a, b, c;
    scanf("%d %d", &a, &b);
    int number[a];
    int input[a];

    for (int i = 0; i < a; i++) {
        number[i] = i + 1;
    }

    for (int i = 0; i < b ; i++) {
        scanf("%d", &c);
        input[i] = c;
    }
    for (int i = 0; i < a; i++) {
        if input[i] = 
    }



    return 0;
}