#include <iostream>
#include <queue>
#include <vector>
#include <list>
#include <map>
using namespace std;

int main () {
    int N;
    queue<int> Q;
    map<int, bool> vis;
    list<int> li;
    for (int i : li) Q.push(i);

    while(!Q.empty()) {
        
    }

    return 0;
}